{
  "name": "custom-hostnames-custom-metadata",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230307.0",
    "prettier": "^2.8.6",
    "typescript": "^5.0.2",
    "wrangler": "2.12.3"
  },
  "private": true,
  "scripts": {
    "format": "prettier --write 'src/**/*.{js,ts}'",
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  }
}
